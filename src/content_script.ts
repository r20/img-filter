// Not doing anything anymore with content_scripts.

// let [tab] = await chrome.tabs.query({
//   active: true,
//   currentWindow: true,
// });

// // An empty export is needed to allow await to work
// export {};
